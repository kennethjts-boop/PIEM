{
  "buildCommand": "cd profeia/client && npm install && npm run build",
  "outputDirectory": "profeia/client/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
